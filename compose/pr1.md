1. 问题，未检查不存在的服务。

project/project.go
func traverse

        for  select,_ := range selected {
                if wrappers[temp] == nil {
                     return errors.New("No such service: " + temp)
                }
        }
